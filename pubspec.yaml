name: omni_sms
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'  # ✅ LARGE COMPATIBILITÉ Dart 3.5.x/3.6.x
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.2
  firebase_core: ^3.4.0      # ✅ Stable
  firebase_messaging: ^15.0.0
  google_mobile_ads: ^4.0.0  # ✅ Dart 3.5.x OK
  flutter_sound: ^9.2.13
  record: ^5.1.0
  shared_preferences: ^2.2.3
  firebase_auth: ^5.1.4
  url_launcher: ^6.2.6
  web_socket_channel: ^3.0.1
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"
