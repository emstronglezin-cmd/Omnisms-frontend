name: omni_sms
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <3.7.0'      # ✅ Dart 3.6.0 OK + google_mobile_ads
  flutter: '>=3.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  http: ^1.2.2
  firebase_core: ^3.6.0        # ✅ Stable Dart 3.6
  firebase_messaging: ^15.1.3  # ✅ Stable Dart 3.6
  google_mobile_ads: ^5.1.0    # ✅ Dart 3.6.0 OK (PAS 7.0.0)
  flutter_sound: ^9.2.13       # ✅ Stable
  record: ^5.1.2               # ✅ Stable
  shared_preferences: ^2.3.2
  firebase_auth: ^5.3.1        # ✅ Stable
  url_launcher: ^6.3.1
  web_socket_channel: ^3.0.1
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0        # ✅ Dart 3.6 OK

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"
